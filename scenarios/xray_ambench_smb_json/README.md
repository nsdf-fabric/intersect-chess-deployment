# Xray AMBench SMB JSON Scenario

This scenario runs the xray AMBench SMB workflow with the reduced JSON fixture.

## Run

```bash
docker compose -f scenarios/xray_ambench_smb_json/docker-compose.yml up -d --force-recreate
```

## Campaign payload

`scenarios/xray_ambench_smb_json/campaign.json`
